actual todo:
- make the preview overlay closer to truth
- handle wrapping for angle and letter-spacing changes
- error handling(few error handlers implemented, they raise but not handled on the front-end)
- make it possible to scale the time bar
- apply karaoke format of ASS or make my own (depend on the ASS version)
- handle SRT and LRC formats
- (save progress) at the init, load the data from uploads so the user can work with the last used video if not finished working on it.
- after the above, add a cleanup button so user can work on a new file.
- make timestamped lines more readable. For example when the mouse hover's over the timestamp, it then shows the text instead of simply showing it right now coz it can make a mess with fast changing texts.
- make it possible to render 2 lines at the same time, rap songs or other fast ones case
- make it possible to switch between instrumental and vocal version of audio
- add fonts so ffmpeg gets more fonts to choose from
- build own removal model
- bug: line displaying dynamic scaling acts weird when the window is getting too small (like phone size)
- uploads stay in the "cache".
- !may not be a problem; If video gets deleted, it stays in the cache but it may generate problems with rendering later on.\
    Generates problem with vocal removal - file not found. Add a fix that gets the video again from the cache.

tests:
- check if something happens when a "big" sized file is uploaded
after everything's ready:
- set debug=false
- adapt port to the available choices
